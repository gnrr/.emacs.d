;; ----------------------------------------------------------------------
;; discrete.el
;; ----------------------------------------------------------------------

;; ----------------------------------------------------------------------
;; test
;; ----------------------------------------------------------------------
(defun my-kill-buffer ()
  (interactive)
  (kill-buffer (current-buffer)))

(global-set-key (kbd "C-x k") 'my-kill-buffer)
;; ----------------------------------------------------------------------
;; test
;; ----------------------------------------------------------------------
(defun test ()
  (interactive)
  (message "test!"))

